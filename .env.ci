# Deprecated CI environment file
#
# This file previously duplicated `.env.example` with identical content.
# `.env.example` is now the single source of truth for environment variables.
#
# CI pipelines copy `.env.example` to `.env` for Docker Compose builds.
# If you need CI-specific overrides, define ONLY those overrides here.
